{
  "name": "@admitto/db",
  "version": "0.0.1",
  "private": true,
  "description": "Admitto DB — Prisma schema + client (PostgreSQL)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./status": {
      "types": "./dist/status.d.ts",
      "default": "./dist/status.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "npm run build",
    "precoverage": "npm run pretest",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "postinstall": "prisma generate",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy && tsx src/scripts/backfill-public-ref.ts",
    "db:migrate:dev": "prisma migrate dev",
    "db:migrate:status": "prisma migrate status",
    "predb:seed": "npm run build --workspace=@admitto/crypto",
    "db:seed": "prisma db seed",
    "db:backfill-public-ref": "tsx src/scripts/backfill-public-ref.ts"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@admitto/crypto": "*",
    "@prisma/client": "^5.17.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "prisma": "^5.17.0",
    "tsx": "^4.16.0",
    "typescript": "^6.0.3",
    "vitest": "4.1.9"
  }
}
