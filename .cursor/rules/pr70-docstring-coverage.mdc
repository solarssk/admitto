---
description: PR #70 follow-up — CodeRabbit docstring coverage gate (80%)
globs: apps/web/src/admin/attendees-api-routes.ts,apps/web/src/admin-resend-rate-limit.ts,packages/mail-delivery/src/toDeliveryDto.ts,packages/mail-delivery/src/resend.ts,packages/mail-delivery/src/listDeliveries.ts,packages/tickets/src/ops-audit.ts
alwaysApply: false
---

# PR #70 — docstring coverage (queued commit)

Before the next push on `feature/admin-attendees-screen` / PR #70, fix the CodeRabbit check:

- **Check:** Docstring Coverage — currently ~18%, required **80%**
- **Action:** Add JSDoc (`/** ... */`) on **exported** functions, handlers, and public types in new/changed backend modules from v0.4.2a attendees work

Priority files (new in PR #70):

- `apps/web/src/admin/attendees-api-routes.ts` — route handlers + shared helpers
- `apps/web/src/admin-resend-rate-limit.ts` — `createAdminResendRateLimit`
- `packages/mail-delivery/src/toDeliveryDto.ts` — `toDeliveryDto`, `DeliveryDto`
- Updated exports in `resend.ts`, `listDeliveries.ts` if still flagged

Match existing repo style (see `packages/tickets/src/admit.ts`, `packages/auth` exports): one-line summary + `@param` / `@returns` where non-obvious.

Do **not** add docstrings only to React components unless the gate flags them — this check targets TS backend exports.

Verify locally if possible; otherwise confirm the warning is cleared on the PR before merge.
