name: CI

on:
  push:
    branches: [main]
  # No `branches:` filter here on purpose: stacked PRs (native GitHub stacks) have a base ref
  # that is another feature branch, not main, once the bottom-most PR in the stack has merged
  # and closed - GitHub's "evaluate as if targeting main" virtualization needs an unbroken chain
  # of open PRs down to main, and a merged/closed PR severs that chain. Restricting to
  # `branches: [main]` silently skipped required-status-check jobs (build-test, secret-scan,
  # pii-guard, analyze, migration-safety) for every PR in a stack after its first link merged.
  pull_request: {}

# Cancel a still-running CI for the same PR/branch the moment a newer push supersedes it - without
# this, every push to an open PR queues a brand new set of jobs alongside the previous (now
# pointless) run instead of replacing it, wasting runner slots and delaying feedback on the latest
# commit. Not applied to `main`: a push there gates docker-build (image publish) and is rare enough
# (one per merge) that every commit deserves a full, uncancelled run rather than racing the next.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

env:
  # Shared by every job below (only test-web/test-admin/test-rest actually use these; harmless
  # unused strings in lint-and-typecheck). Defined once at workflow level, not per-job.
  # No password: the Postgres service containers below run with POSTGRES_HOST_AUTH_METHOD=trust
  # (network-unreachable outside this job's own steps, so there's nothing to protect with a
  # password) — closes SonarCloud's hardcoded-secret rule (secrets:S6698) without a repo secret,
  # which would resolve to empty (and fail Postgres startup) on a fork-originated pull_request run
  # since GitHub withholds secrets from those by default (caught by Codex review on PR #570).
  DATABASE_URL: postgresql://admitto@localhost:5432/admitto
  REDIS_URL: redis://localhost:6379
  # Fixed test key — 32 bytes. Not a production secret; used only in CI test runs.
  ENCRYPTION_KEY: AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=

jobs:
  wiki-docs:
    # Dependabot PRs omit the human/agent PR template body. Skip this gate by PR author, not
    # github.actor: a human re-run sets actor to a person and would otherwise re-enable the
    # documentation-impact check against Dependabot's body. Push to main still runs as usual.
    if: >
      github.event_name != 'pull_request' ||
      github.event.pull_request.user.login != 'dependabot[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "24.17.0"
      - name: Validate Wiki source
        run: npm run docs:check
      - name: Validate pull request documentation impact
        if: github.event_name == 'pull_request'
        run: npm run docs:pr-check

  # build-test used to be one sequential job (~7-8 min, mostly the coverage step across all 14
  # workspaces). Split into 4 parallel jobs so they run concurrently instead of one after another
  # - free on this public repo (unlimited Actions minutes), only wall-clock time is at stake.
  # apps/web and apps/admin each get their own job since together they're ~75%+ of total test
  # volume; everything else is bundled into test-rest.
  lint-and-typecheck:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "24.17.0"
          cache: "npm"
      # setup-node's cache: npm only caches the ~/.npm download store, not the linked node_modules
      # tree or the native-binding rebuild below - both still redone from scratch every run. This
      # cache lets an unchanged lockfile skip `npm ci` + `npm rebuild` entirely across runs.
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        id: node-modules-cache
        with:
          path: |
            node_modules
            packages/*/node_modules
            apps/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
      # Supply-chain hardening (SonarCloud githubactions:S6505): --ignore-scripts skips every
      # dependency's install/postinstall script by default, instead of letting all ~900 of them
      # run silently and automatically. Only the packages this workflow has confirmed it actually
      # needs get their scripts run explicitly below — esbuild (its own binary, needed by every
      # vite/vitest/tsx step) and argon2 (its native password-hashing binding, needed by
      # packages/auth) via `npm rebuild`, and Prisma's query engine + generated client via its own
      # `prisma generate`. Adding a new dependency that also needs its install script to run means
      # adding it here explicitly, not silently inheriting it.
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci --ignore-scripts
      - name: Verify lockfile resolutions match package.json declarations
        run: python3 scripts/verify-lockfile-resolutions.py
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm rebuild esbuild argon2 @prisma/engines prisma @prisma/client
      - run: npm run db:generate -w @admitto/db
      - run: npm audit --audit-level=high
      - name: License compliance check (report-only - see scripts/check-licenses.mjs)
        continue-on-error: true
        run: node scripts/check-licenses.mjs
      - run: npm run build
      - name: Release doc version markers match package.json
        run: python3 scripts/sync-release-docs.py --check
      - run: npm run lint
      - run: npm run typecheck --workspaces --if-present

  test-web:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: admitto
          POSTGRES_HOST_AUTH_METHOD: trust
          POSTGRES_DB: admitto
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "24.17.0"
          cache: "npm"
      # See lint-and-typecheck's own comment for why the cache step and the two conditional steps
      # below exist.
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        id: node-modules-cache
        with:
          path: |
            node_modules
            packages/*/node_modules
            apps/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci --ignore-scripts
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm rebuild esbuild argon2 @prisma/engines prisma @prisma/client
      - run: npm run db:generate -w @admitto/db
      # Not safe to drop in favor of @admitto/web's own precoverage/pretest hook: that hook (and
      # several others across the monorepo, e.g. apps/admin's) only lists a subset of its real
      # transitive @admitto/* dependencies, or lists them in the wrong build order - they were
      # never designed to be self-sufficient, only to top up a workspace already built by this
      # root build once (a build with no @admitto/* rebuild needed is a fast no-op for tsc).
      # Confirmed by removing this step: apps/db's own build failed on a missing @admitto/shared
      # type (apps/admin's pretest builds @admitto/db before @admitto/shared, the reverse of what
      # @admitto/db's own build needs), and apps/web's pretest builds @admitto/ui directly without
      # first building crypto/db/location/wallet/mail-templates/tickets/auth that @admitto/ui
      # itself depends on. Don't repeat this optimization attempt without first making every
      # workspace's own pretest/precoverage chain independently, correctly topologically complete.
      - run: npm run build
      - name: Create test databases
        run: bash infra/scripts/create-test-dbs.sh
      - name: Migrate web integration test database
        env:
          DATABASE_URL: postgresql://admitto@localhost:5432/admitto_web_test
        working-directory: packages/db
        # --no-install: use only the already-installed, lockfile-pinned prisma binary, never an
        # on-demand npx download (SonarCloud githubactions:S6505 / S8543).
        run: npx --no-install prisma migrate deploy
      - name: Run coverage (also emits JUnit XML for Test Analytics)
        run: npm run coverage -w @admitto/web --if-present -- --reporter=default --reporter=junit --outputFile.junit=./junit.xml
      - name: Upload coverage to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: web
          files: |
            apps/**/coverage/lcov.info
            apps/**/coverage-integration/lcov.info
          fail_ci_if_error: false
      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@0fa95f0e1eeaafde2c782583b36b28ad0d8c77d3  # v1.2.1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: web
          fail_ci_if_error: false

  test-admin:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "24.17.0"
          cache: "npm"
      # See lint-and-typecheck's own comment for why the cache step and the two conditional steps
      # below exist.
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        id: node-modules-cache
        with:
          path: |
            node_modules
            packages/*/node_modules
            apps/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci --ignore-scripts
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm rebuild esbuild argon2 @prisma/engines prisma @prisma/client
      - run: npm run db:generate -w @admitto/db
      # Not safe to drop in favor of @admitto/admin's own precoverage/pretest hook - see
      # test-web's own comment on its equivalent build step for why (found the hard way: this
      # exact job failed on it first).
      - run: npm run build
      - name: Run coverage (also emits JUnit XML for Test Analytics)
        run: npm run coverage -w @admitto/admin --if-present -- --reporter=default --reporter=junit --outputFile.junit=./junit.xml
      - name: Upload coverage to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: admin
          files: |
            apps/**/coverage/lcov.info
            apps/**/coverage-integration/lcov.info
          fail_ci_if_error: false
      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@0fa95f0e1eeaafde2c782583b36b28ad0d8c77d3  # v1.2.1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: admin
          fail_ci_if_error: false

  # Every remaining workspace. packages/db and packages/tickets both target the same
  # admitto_tickets_test database and both destructively reset its schema in their own beforeAll
  # (prisma migrate reset / db push --force-reset) - they must stay in this one job, never split
  # into separate concurrent jobs against each other, or a schema reset can race a live query in
  # the other package's suite. Every other workspace here (auth/import/mail-delivery/
  # mail-templates/mailer/mailer-config/crypto/ui/cli) already has its own uniquely-named test
  # database (or none at all), verified with an Explore agent before this split was written.
  test-rest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: admitto
          POSTGRES_HOST_AUTH_METHOD: trust
          POSTGRES_DB: admitto
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: "24.17.0"
          cache: "npm"
      # See lint-and-typecheck's own comment for why the cache step and the two conditional steps
      # below exist.
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        id: node-modules-cache
        with:
          path: |
            node_modules
            packages/*/node_modules
            apps/*/node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm ci --ignore-scripts
      - if: steps.node-modules-cache.outputs.cache-hit != 'true'
        run: npm rebuild esbuild argon2 @prisma/engines prisma @prisma/client
      - run: npm run db:generate -w @admitto/db
      # Not safe to drop in favor of each tested workspace's own precoverage/pretest hook - see
      # test-web's own comment on its equivalent build step for why.
      - run: npm run build
      - name: Create test databases
        run: bash infra/scripts/create-test-dbs.sh
      - name: Run database migrations and agency public_ref backfill
        run: npm run db:migrate -w @admitto/db
      - name: Migrate auth integration test database
        env:
          DATABASE_URL: postgresql://admitto@localhost:5432/admitto_auth_test
        working-directory: packages/db
        # --no-install: use only the already-installed, lockfile-pinned prisma binary, never an
        # on-demand npx download (SonarCloud githubactions:S6505 / S8543).
        run: npx --no-install prisma migrate deploy
      - name: Run coverage (also emits JUnit XML per workspace for Test Analytics)
        run: >-
          npm run coverage --if-present
          -w @admitto/db -w @admitto/tickets -w @admitto/auth -w @admitto/import
          -w @admitto/mail-delivery -w @admitto/mail-templates -w @admitto/mailer
          -w @admitto/mailer-config -w @admitto/crypto -w @admitto/storage -w @admitto/location -w @admitto/ui -w @admitto/cli
          -w @admitto/shared
          -- --reporter=default --reporter=junit --outputFile.junit=./junit.xml
      - name: Upload coverage to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f  # v7.0.0
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: rest
          files: |
            packages/**/coverage/lcov.info
            packages/**/coverage-integration/lcov.info
            apps/**/coverage/lcov.info
            apps/**/coverage-integration/lcov.info
          fail_ci_if_error: false
      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@0fa95f0e1eeaafde2c782583b36b28ad0d8c77d3  # v1.2.1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: rest
          fail_ci_if_error: false

  # main's branch protection requires a status check literally named "build-test" - this
  # aggregator keeps that name alive (and its pass/fail contract equivalent to the old monolithic
  # job) without needing anyone to update the protection rule's required-checks list. Update this
  # `needs:` list, not the branch protection settings, if the shard jobs above are ever renamed.
  #
  # if: always() + an explicit result check, not the needs: default - by default a job whose
  # needs: includes a failed job is skipped, not failed, and this aggregator is the one thing
  # standing between a shard's real outcome and the required check GitHub actually gates merges
  # on. Relying on "skipped never satisfies a required check" is the kind of implicit contract
  # worth not staking a branch-protection guarantee on when an explicit, always-run check is this
  # cheap (CodeRabbit review).
  build-test:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    needs: [lint-and-typecheck, test-web, test-admin, test-rest]
    if: always()
    steps:
      - name: Check all shards succeeded
        run: |
          for result in \
            "${{ needs.lint-and-typecheck.result }}" \
            "${{ needs.test-web.result }}" \
            "${{ needs.test-admin.result }}" \
            "${{ needs.test-rest.result }}"; do
            if [ "$result" != "success" ]; then
              echo "At least one build-test shard did not succeed."
              exit 1
            fi
          done
          echo "All build-test shards passed."

  secret-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: gitleaks
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_REF: ${{ github.base_ref }}
          BEFORE_SHA: ${{ github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          curl -sSfL --proto '=https' --proto-redir '=https' -o gitleaks.tar.gz \
            https://github.com/gitleaks/gitleaks/releases/download/v8.18.4/gitleaks_8.18.4_linux_x64.tar.gz
          echo "ba6dbb656933921c775ee5a2d1c13a91046e7952e9d919f9bac4cec61d628e7d  gitleaks.tar.gz" | sha256sum -c -
          tar -xzf gitleaks.tar.gz gitleaks
          # gitleaks' default log-opts is `git log --all --full-history` - with fetch-depth:0
          # above fetching every remote branch, that scans every open branch's commits, not just
          # this ref's own. Any PR then fails secret-scan if ANY other branch in the repo has a
          # commit gitleaks flags, regardless of that PR's actual changes. Scope the scan to just
          # this ref's own commits instead: the PR's commits since its base, or the commits a push
          # actually introduced.
          if [ "$EVENT_NAME" = "pull_request" ]; then
            RANGE="origin/$BASE_REF..HEAD"
          elif [ -n "$BEFORE_SHA" ] && [ "$BEFORE_SHA" != "0000000000000000000000000000000000000000" ]; then
            RANGE="$BEFORE_SHA..$HEAD_SHA"
          else
            RANGE="-1"
          fi
          ./gitleaks detect --source . --no-banner --redact --exit-code 1 --log-opts="$RANGE"

  pii-guard:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: No real email addresses in tracked data files
        run: |
          # -z / xargs -0: handle filenames with spaces correctly
          # domain anchor ($): prevent @example.com.evil bypass
          # package-lock.json may contain maintainer emails in npm deprecation metadata — not our data.
          HITS=$(git ls-files -z -- '*.csv' '*.tsv' '*.json' ':!package-lock.json' \
            | xargs -0 grep -hREo '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' -- \
            | grep -viE '@(example\.(com|org|net)|test\.(com|org|net))$' || true)
          if [ -n "$HITS" ]; then
            echo "Suspicious real email addresses found in data files:"
            echo "$HITS"
            exit 1
          fi
          echo "OK: no real email addresses in data files"
      - name: No phone-number-shaped strings in tracked data files
        run: |
          # E.164-leaning: a leading "+" (rare outside phone numbers) followed by 7-21 real
          # digits, allowing space/dot/paren/dash separators grouped in runs of at most 2 between
          # digits (covers a US-style grouping - country code, then an area code wrapped in
          # parens followed by a space, then a dashed local number - without letting separators
          # pad out the count). Each unit past the first digit is exactly one digit, optionally
          # preceded by up to 2 separator chars - so {6,20} counts real digits, not characters: a
          # naive `[0-9 .()-]{5,19}` (the first version of this check) let separator padding
          # satisfy the length requirement with almost no real digits - a "+" followed by one
          # digit, five dashes, and one more digit used to match despite having only 2 real
          # digits (caught by review on PR #1158 before this shipped). The (^|[^0-9]) / ($|[^0-9])
          # boundary groups require a non-digit (or line start/end) on both sides, so this does
          # not fire on a "+" that is itself part of a longer digit run - eg. semver build
          # metadata like "1.0.0+20130313144700", or a big numeric id - the main false-positive
          # class for naive phone regexes. A "+"-prefixed decimal value can still match if its
          # digit count alone happens to land in phone-number range - digit count alone can't
          # distinguish that from a short real phone number, and no separator-shape fix changes
          # that; unchanged from the original design intent, not a regression. Wider scope than
          # the email check above (also .yaml/.yml/.sql): measured zero false positives against
          # this repo's current tracked files in that scope before wiring this in as a blocking
          # check. NOTE FOR FUTURE EDITS: this step scans .yml files too, including this workflow
          # file itself - do not put a literal phone-shaped example in a comment near this step,
          # it will match its own check (this is exactly how the previous comment revision broke
          # CI; describe examples in prose instead of a quoted literal).
          HITS=$(git ls-files -z -- '*.csv' '*.tsv' '*.json' '*.yaml' '*.yml' '*.sql' ':!package-lock.json' \
            | xargs -0 grep -hREo '(^|[^0-9])\+[1-9]([0-9]|[ .()-]{1,2}[0-9]){6,20}($|[^0-9])' -- || true)
          if [ -n "$HITS" ]; then
            echo "Suspicious phone numbers found in data files:"
            echo "$HITS"
            exit 1
          fi
          echo "OK: no phone-number-shaped strings in data files"

  migration-safety:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      pull-requests: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: PR destructive override label
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh pr view "${{ github.event.pull_request.number }}" \
            --json labels -q '.labels[].name' \
            | grep -qx 'migration-destructive-approved'; then
            echo "MIGRATION_DESTRUCTIVE_LABEL_APPROVED=1" >> "$GITHUB_ENV"
          fi
      - name: Scan new migrations for destructive SQL
        run: bash scripts/check-migrations-destructive.sh
      - name: Migration guard unit tests
        run: bash scripts/check-migrations-destructive.test.sh
      - name: Migrate entrypoint smoke (docker compose)
        run: bash scripts/test-migrate-entrypoint.sh

  # Complements `npm audit --audit-level=high` in lint-and-typecheck: that check re-audits the
  # whole resolved tree on every run regardless of what changed, while this action diffs the
  # dependency manifest against the PR's base ref, so it only flags newly introduced vulnerable or
  # license-incompatible packages - the thing a PR actually did. pull_request-only by design (the
  # action needs a base ref to diff against; nothing to compare on a push to main).
  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294  # v5.0.0
        with:
          fail-on-severity: high
          # `fail-on-severity` alone only gates vulnerabilities - it does nothing for license
          # compliance, so without `allow-licenses` a newly introduced disallowed-license
          # dependency would pass this job silently (scripts/check-licenses.mjs's own gate for
          # that is `continue-on-error: true`, i.e. non-blocking, so nothing else here catches it
          # either). Mirrors that script's ALLOWLIST of standalone permissive SPDX licenses -
          # spdx-satisfies (what this action uses) resolves an "X OR Y" compound automatically once
          # one side is allowed, so the compound entries in ALLOWLIST don't need listing here too,
          # but an "X AND Y" compound needs every side present (hence adding Zlib alongside MIT for
          # the one AND-compound package that script allows). Keep in sync with
          # scripts/check-licenses.mjs's ALLOWLIST by hand; nothing enforces that automatically.
          #
          # Deliberately NOT mirroring check-licenses.mjs's REVIEWED_PACKAGE_VERSIONS via this
          # action's `allow-dependencies-licenses`: that input matches on package name only - this
          # action's own `purlsMatch()` explicitly ignores the version part of a purl, so a
          # version-qualified entry would silently exempt every future version too, defeating the
          # exact thing REVIEWED_PACKAGE_VERSIONS exists for (forcing re-review on a version bump -
          # see that script's own comment). Confirmed this isn't theoretical: right now
          # @img/sharp-libvips-darwin-arm64@1.3.3 and @img/sharp-wasm32@0.35.4 are already past
          # their reviewed 1.3.2/0.35.3 entries and would fail scripts/check-licenses.mjs today if
          # it were blocking. Leaving these packages off `allow-licenses` means a PR that bumps one
          # of them will correctly fail this gate until a human re-reviews and updates both this
          # list and REVIEWED_PACKAGE_VERSIONS - friction that's the point, not a bug.
          allow-licenses: MIT, ISC, Apache-2.0, BSD-3-Clause, BSD-2-Clause, BlueOak-1.0.0, Unlicense, OFL-1.1, MIT-0, CC0-1.0, 0BSD, CC-BY-4.0, Python-2.0, Zlib

  docker-build:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      security-events: write
    needs: [build-test, migration-safety]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false
      - name: Build production image
        run: docker build -f Dockerfile -t admitto-app .

      # Report-only: catches base-image CVE drift on every main build instead of waiting for the
      # next release tag, where publish-container.yml's Trivy step still does the blocking
      # CRITICAL gate. Same tool/settings, exit-code 0 here since nothing downstream depends on
      # this build passing or failing on vulnerabilities.
      - name: Trivy vulnerability scan (SARIF, report-only)
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25  # v0.36.0
        with:
          image-ref: admitto-app
          format: sarif
          output: trivy-results.sarif
          scanners: vuln
          vuln-type: os,library
          ignore-unfixed: true
          severity: CRITICAL,HIGH
          limit-severities-for-sarif: true
          exit-code: 0

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@cdf488f595d80d6e07e03d4674febd5ab45fa938  # v4
        if: always()
        with:
          sarif_file: trivy-results.sarif
          category: trivy-container-main
