name: PR-Agent

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  pr_agent:
    runs-on: ubuntu-latest
    if: >-
      ${{
        github.event.sender.type != 'Bot' &&
        github.event.issue.pull_request &&
        contains(fromJSON('["COLLABORATOR","MEMBER","OWNER"]'), github.event.comment.author_association) &&
        (
          startsWith(github.event.comment.body, '/review') ||
          startsWith(github.event.comment.body, '/describe') ||
          startsWith(github.event.comment.body, '/improve') ||
          startsWith(github.event.comment.body, '/ask')
        )
      }}
    steps:
      - uses: Codium-ai/pr-agent@31d7dd027968e5fad1f9cbb074be047c4869058e  # HEAD 2026-06-08
        env:
          ANTHROPIC__KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          config__model: "anthropic/claude-sonnet-4-6"
          config__fallback_models: '["anthropic/claude-sonnet-4-6"]'
